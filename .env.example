# ============================================
# Anthropic API 配置
# ============================================

# Anthropic API 密钥（必需）
# 获取方式：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Anthropic API 基础 URL（可选）
# 默认：https://api.anthropic.com
ANTHROPIC_BASE_URL=

# 启用自定义 HTTP Headers（可选）
# 默认：false
# 说明：启用后会在 Anthropic API 请求中注入预定义的 headers
#      包括 anthropic-beta、user-agent、x-stainless-* 等
# 注意：x-api-key 始终从 ANTHROPIC_API_KEY 读取，不受此配置影响
ANTHROPIC_CUSTOM_HEADERS_ENABLED=false


# ============================================
# HTTP 服务器配置（examples/http）
# ============================================

# HTTP 服务器监听地址
# 默认：:8080
AGENTSDK_HTTP_ADDR=:8080

# 默认使用的模型名称
AGENTSDK_MODEL=claude-sonnet-4-5-20250929

# 默认请求超时时间
# 默认：45s
# 支持格式：45s, 1m, 2m30s, 90000（毫秒）
AGENTSDK_DEFAULT_TIMEOUT=45s

# 最大并发会话数
# 默认：500
AGENTSDK_MAX_SESSIONS=500

# 项目根目录（可选）
# 默认：自动解析
# 说明：用于容器或 CI 环境显式指定项目路径
AGENTSDK_PROJECT_ROOT=
